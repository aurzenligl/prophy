import aprot

class {{name}}(aprot.struct):
	__metaclass__ = aprot.struct_generator
	_descriptor = [{%- for key,value in elements.items() -%}
	 			   {%- if not '0' in value[1] -%}
	 			   ('{{key}}', aprot.bytes(size = {{value[1]}}))
	 			   {%- else -%}
	 			   ('{{key}}',{{value[0]}})
	 			   {%- endif -%}
	 			   {%- if loop.last -%}
	 			   ]
	 			   {%- else -%},{%- endif -%}
	 {% endfor %}
	
	
