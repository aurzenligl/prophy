{%- macro render_dialog(key,value,key_value,value_value) -%}
    {{ key }} == {{value}}
    {{ key_value }} == {{value_value}}
    {{ caller() }}
{%- endmacro -%}

class {{item_name}}(aprot.struct):
	__metaclass__ = aprot.struct_generator
	_descriptor = [{% for key,value in item_value.items() -%}
	 			   {% for key_value,value_value in value.items() -%}
	 			   {% call render_dialog(key,value,key_value,value_value) %}
	 			   {% endcall %}
	 			   {% endfor %}
	 {% endfor %}


